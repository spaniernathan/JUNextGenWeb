<section class="section is-small">
    <h1 class="title">Playlists</h1>
    <div class="container">
        <div class="columns is-multiline">
            {{#each playlists}}
            <div class="column is-3">
                {{>playlistCard content=this}}
            </div>
            {{/each}}
        </div>
    </div>
</section>
